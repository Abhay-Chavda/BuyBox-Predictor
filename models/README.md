# Model artifacts

The public portfolio version uses a synthetic demonstration model generated by `scripts/train_demo_model.py`.

The original internship model and company data are not included in this repository.

Generate the demo artifact locally with:

```bash
python scripts/train_demo_model.py
```
